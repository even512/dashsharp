{
  "name": "homelab-dashboard",
  "version": "0.2.7",
  "private": true,
  "description": "Schlankes Homelab-Dashboard als Browser-Startseite (Node.js + Express, Docker)",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "test": "node scripts/smoke-test.mjs",
    "lint": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "compression": "^1.8.1",
    "express": "^4.19.2",
    "js-yaml": "^4.1.0",
    "ssh2": "^1.16.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "sharp": "^0.35.2"
  }
}
